{
  "name": "frittz-productos-de-la-costa",
  "version": "2.0.0",
  "description": "Panel de administración y reparto - Optimizado sin Babel",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
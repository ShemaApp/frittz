{
  "name": "productos-de-la-costa",
  "version": "2.0.0",
  "description": "Sistema de gestión optimizado - Sin Babel en navegador",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "deploy": "npm run build && echo '✅ Listo para desplegar'"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}